UsersTableIAM:
  Effect: Allow
  Action:
    - dynamodb:*
  Resource: 
    - ${self:custom.UsersTable.Arn}
    - !Join ['/', ['${self:custom.UsersTable.Arn}', 'index', 'emailIndex']]
    # - !Join ['/', ['${self:custom.UsersTable.Arn}', 'index', 'usernameIndex']]
    # - !Join ['/', ['${self:custom.UsersTable.Arn}', 'index', 'fullnameIndex']]
